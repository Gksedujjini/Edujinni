@model Edujinni.Models.addclass
@{
//   Layout = null;
}
@using (Html.BeginForm())
{
    <!DOCTYPE html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>AddAchievements</title>
        <script src="https://code.jquery.com/jquery-3.3.1.js"
                integrity="sha256-2Kok7MbOyxpgUVvAk/HJ2jigOSYS2auK4Pfzbm7uH60="
                crossorigin="anonymous"></script>
        <script src="~/Scripts/jquery.validate.js"></script>
        <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
        <script src="https://code.jquery.com/jquery-3.3.1.js"
                integrity="sha256-2Kok7MbOyxpgUVvAk/HJ2jigOSYS2auK4Pfzbm7uH60="
                crossorigin="anonymous"></script>
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" />
        <style>
            #achievements {
                height: 45px;
                width: 250px;
                border-radius: 20px;
            }

            p {
                text-align: center;
            }

            #vertical {
                margin-top: 120px;
                margin-bottom: 200px;
            }
        </style>
        @*<script>
                $(document).ready(function () {
                    $.getJSON("http://www.edujinni.in/classNames/classDetailsList/",function(data)
                    {
                        $.each(data,function(key,val){
                            $("<li>"+val+"<li>").appendTo($("#ach"));
                        })

                    })
                    })
            </script>*@
    </head>
    <body>
            <div id="vertical">
                <p style="font-size:40px;">Add Class</p>
                @*<select id="ach">
                </select>*@
                <br />
                //Select teacher is shown here...code exists
                @*@Html.DropDownList("teacher", ViewBag.class_name as SelectList, "Select Teacher", new { @class = "form-control", @id = "achievements" })*@
                <br />
                @Html.TextBoxFor(Model => Model.class_name, new { @class = "form-control", @placeholder = "Enter Class Name", @id = "achievements" })
                @Html.ValidationMessageFor(Model => Model.class_name, "", new { @class = "text-danger" })
                <br />
                @{ 
                    List<SelectListItem> list = new List<SelectListItem>();
                }
                @Html.DropDownListFor(Model => Model.class_section_name, list, new { @class = "form-control", @placeholder = " Select Section Name", @id = "achievements" })
                @*@Html.DropDownList(Model => Model.class_section_name, new { @class = "form-control", @placeholder = "Enter Section Name", @id = "achievements" })*@
                @Html.ValidationMessageFor(Model => Model.class_section_name, "", new { @class = "text-danger" })
                <br />
                <input type="submit" value="Add" class="btn btn-success" id="achievements" />    
            </div>
    </body>
}